git clone [url du github]
git pull
git push
git commit -a -m "[description de la modification]"
git status
git add .

javac -sourcepath . Main.java
Java Main
rm *.class

javac -sourcepath . Main.java && Java Main
rm *.class; git commit -a -m "cohérence de des déplacement avec l'animation de l'arrière plan" && git pull && git push;